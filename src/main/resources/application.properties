spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.properties.hibernate.dialect= org.hibernate.dialect.PostgreSQLDialect
spring.session.jdbc.initialize-schema=always
spring.jpa.hibernate.ddl-auto=update

spring.security.user.name=admin
spring.security.user.password=admin

spring.main.allow-bean-definition-overriding=true
logging.level.org.hibernate=ERROR

spring.application.name= general-service

#Eureka server
eureka.client.service-url.defaultZone=${Eureka_Server}


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME}
spring.mail.password=${EMAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.debug=true

logging.path=./logs

jwt.google.secret-key=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
jwt.secret-key=${JWT_SECRET_KEY}
jwt.secret-key-service=${JWT_SERVICE_SECRET}
jwt.expires-time=${JWT_EXPIRES_TIME}
system.request.method=${METHOD}

#Redirect URL
ui.register.success=${REGISTER_SUCCESS}
ui.forgot.password =${FORGOT_PASSWORD}

authentication.whitelistedUris=${WHITE_LIST_URI}